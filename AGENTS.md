# Agent instructions

See `CLAUDE.md`. It is the single source of truth for this repo: commands, the startup path, layering, and the issue and triage conventions.

Rules cited in review live in `CODING_STANDARDS.md`. Domain vocabulary, including the words to avoid, lives in `CONTEXT.md`.

Working inside `web/`, read `web/AGENTS.md` first. Next.js writes and re-adds that block itself.
